let bonus = 20;

function sum(first, second) {
    let result = first + second + bonus;
    return result;
}
const output = sum(3, 7);
console.log(bonus);
console.log(output);

// must see the video another tym(23-7);